VAULT_RECOVERY_KEYS="<%= scope.lookupvar('profile::vault::recovery_keys') %>"
VAULT_ROOT_TOKEN="<%= scope.lookupvar('profile::vault::root_token') %>"
VAULT_API_ADDR="<%= scope.lookupvar('profile::vault::api_addr') %>"
<% unless @additional_env.nil? -%>
<% @additional_env.each do |key,value| -%>
<%= key %>="<%= value %>"
<% end -%>
<% end -%>
